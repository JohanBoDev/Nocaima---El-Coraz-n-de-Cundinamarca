module.exports = {
  content: [
    './src/**/*.{html,js}', // This should cover all HTML and JS files in the src folder
    './index.html',         // Include the index.html in the root folder if it's used
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
